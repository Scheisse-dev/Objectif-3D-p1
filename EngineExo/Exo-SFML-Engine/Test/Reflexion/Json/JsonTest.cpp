#include "JsonTest.h"
#include "../../Z.h"
#include "../../A.h"
#include "../../../Engine/Utils/DebugMacro.h"
#include <filesystem>
#include <fstream>

void Test::JsonTest::Test()
{
    //Z z = Z();
    //A a = A();
    //const std::string _path = std::filesystem::current_path().string();
    //std::ofstream _outFile = std::ofstream(_path + "\\a.asset");
    //std::ifstream _inFile = std::ifstream(_path + "\\a.asset");
    ////a.Serialize(_outFile);
    ////z.DeSerialize(_inFile);
    ////_inFile.close();
    //z.Serialize(_outFile);
    //_outFile.close();

    // cant serielize a deserielize 
    // awnser -> change assez without serielize 

    //A b = A();
    //std::ifstream _inFile(_path + "\\a.asset");
    //b.DeSerialize(_inFile);
    //_inFile.close();
}